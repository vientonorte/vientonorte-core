{
  "name": "vientonorte-core",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo raíz del colectivo vientonorte",
  "engines": {
    "node": ">=20.10",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}
